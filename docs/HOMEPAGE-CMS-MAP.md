# Existing homepage → CMS map

Inspected the actual `/` page in Chrome on 2026-09-23 before implementation, then its source and the existing generic content editor. This task preserves the existing DOM layout/classes, imagery, branding and booking/map components. The user explicitly authorizes this homepage-specific CMS work after the readiness audit.

| PUBLIC SECTION (display order) | PUBLIC COMPONENT | CURRENT DATA SOURCE | PROPOSED ADMIN SECTION | EDITABLE FIELDS | PROTECTED FIELDS | DATABASE SOURCE |
| --- | --- | --- | --- | --- | --- | --- |
| Header | `Header`, `booking.tsx` | Hardcoded labels/links, BrandLogo | Navigation | Five link labels/destinations, booking labels | Logo, menu behavior, language capability, booking handler | Existing content_pages structured homepage document |
| Hero | `Home`, `(public)/page.tsx`, `Media` | Hardcoded lake image, eyebrow, split heading, two-line description, CTAs; optional homepage-hero plain-text override | Homepage / Hero | Existing text, CTA labels and safe links, desktop/mobile images and alt | Layout, emphasis, booking behavior, price | Same document |
| Booking strip | `BookingBar`, `booking.tsx` | Booking domain, availability | Homepage / Booking | None; explain authoritative source | Dates, guests, departures, capacity, booking behavior | Existing booking tables/RPCs |
| A day with possibilities | `Home`, `SectionHeading` | homepage-intro optional override; hardcoded fallback and facts labels; authoritative price/frequency | Homepage / Introduction | Eyebrow, heading, description, tour link | Price, timezone, frequency, booking state, factual fallback labels | Structured document + existing booking projection |
| The places between | `Home`, `RoutePreview` | Illustrative destinations; legacy published-stops branch | Homepage / Places to discover | Heading/copy and editorial destination names, descriptions, images/alt | Route/GPS/stops operational source remains independent map; no geographic fields | Structured document for tourism copy only |
| Four reasons to linger | `Home`, `Media` | Hardcoded destination assets/tags, optional explore content | Homepage / Destination stories | Section heading, shared editorial destination fields, safe guide links | Four-card layout/order | Same document |
| Your day without the guesswork | `Home`, `LiveMapEmbed` | Hardcoded copy + independent Netlify iframe | Homepage / Live map introduction | Eyebrow, split heading, description, link label | Iframe URL, GPS/ETA, reload/recovery UI | Document for copy; existing map for tracking |
| It's your day | `Home` ordered list | Four hardcoded steps | Homepage / How it works | Eyebrow, heading, four titles/descriptions | Numbering and layout | Same document |
| Today at a glance | `Home`, `SectionHeading` | Static heading; shared availability quote | Homepage / Departures introduction | Eyebrow, heading, timetable link label | Departure times, timezone, schedule/empty state | Document + existing booking domain |
| Days worth sharing | `Home` | Review placeholder copy; no real reviews | Homepage / Reviews introduction | Eyebrow, heading, two copy lines | No invented ratings/reviews | Same document |
| The local notebook | `Home`, `Media` | Three hardcoded cards; shared destination images | Homepage / Local notebook | Heading, guide CTA, three eyebrows/titles/CTA labels | Card layout; shared editorial image source | Same document |
| Final booking banner | `Home`, `Media`, `BookButton` | Castle image, split title, eyebrow, optional homepage-final override | Homepage / Final invitation | Image/alt, eyebrow, split heading, button label | Booking handler, preview state | Same document |
| Footer | `Footer`, `ui.tsx` | Hardcoded copy/links, BrandLogo | Footer | Two tagline lines, links, location/contact/language copy | Logo, footer structure | Same document |
| Homepage metadata | `generateMetadata` | homepage-hero SEO or fallback | SEO | Title, description, social image/alt | Indexing gate/canonical environment | Same document |

Current hero: “Discover Shkodra.” / “Your way.”; description “A day between the city, the castle and the lake.” / “Hop on. Wander off. Make it yours.”; image `/images/lake.webp`. Current final image `/images/castle.webp`. Destination images: centre.webp, castle.webp, lake.webp, bridge-view.webp. Existing arrow/target icons are code-controlled, as are section IDs and responsive classes. Exact initial values will be retained in the typed schema/seed, not retyped by the owner.

The existing CMS stores plain-text pages in `content_pages` and has optimistic concurrency and audit logging, but no independent published snapshot or Storage upload/media library was found. Extend this same table with a published snapshot for the structured homepage format; do not create a parallel page builder. Add one Supabase Storage bucket for uploaded editorial images. Preserve plain-text guide records and their editor under a secondary Explore area. Existing operational tables and the independent map are not duplicated.
